//This function will extract the substring after the specified "argument_type"
import Strings.something

extract_after(text, argument_type){
  argument = strip(substring(text, findlast(text, argument_type), text.length))
  return argument
}
